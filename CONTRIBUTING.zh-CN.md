🌐 [English](CONTRIBUTING.md) | 🇨🇳 [中文](CONTRIBUTING.zh-CN.md)

## 如果你在报告 bug

1. 确认你使用的是最新版本。要构建最新的 Xash3D FWGS，请查看 README.md。
2. 检查开放的 issue 确保你的 bug 没有被报告过，检查已关闭的 issue 确认是否已报告并修复。如果已经报告过就不要再发送了。
3. 使用 `-dev 2 -log` 参数重新运行引擎，然后重现 bug 并发布 `engine.log`，该文件可在你的工作目录中找到。
3. 描述重现 bug 的步骤。
4. 描述你使用的操作系统和架构。
6. 如果有助于说明情况，请附加截图。

## 如果你在贡献代码

### 使用哪个分支？

* 我们建议使用 `master` 分支。

### 第三方库

* Uncle Mike 的任何 Xash 项目的理念：不要臃肿。我们也遵循这一点。
* 只有在有**真正**的理由使用时才允许添加新库。如果你能在构建时保留移除新依赖的选项就更好了。
* 不欢迎为 Waf 构建系统添加新依赖。

### 可移植性级别

* Xash3D 有自己的 crt 库。建议使用它。在大多数情况下，它只是标准 C 库的包装器。
* 如果你的功能需要平台特定代码，将其移到 `engine/platform` 并尝试为每个支持的操作系统和编译器实现，或至少留下存根。
* 你必须将其放在适当的宏下。这是一条规则：Xash3D FWGS 必须能在任何地方编译。有关我们支持的平台列表，请参阅 `public/build.h`。

### 代码风格

* 本项目使用 Quake 和 HLSDK 的 C/C++ 代码风格约定的混合。
* 简而言之：
  * 在括号内使用空格，但两个括号连续时不使用。
  * 仅使用制表符进行缩进。
  * 每个大括号必须有自己的行。
  * 使用短块，允许在单行上使用 if 语句和循环。
  * 优先使用 libpublic 中的通用工具，而不是自己实现。
  * 避免魔法数字。
  * 虽然宏很强大，但最好避免过度使用它们。
  * 如果你不确定，尝试模仿引擎源代码中其他地方的代码风格。
* **任何**提交消息都应以声明标签开始，格式：
  
  `tag: added some bugs`
  
  `tag: subtag: fixed some features`
  
  标签可以是：
  * 子系统
  * 简单功能名称或甚至只是文件名（不含扩展名）。
  只要保持它们一致即可，因为这有助于保持提交历史整洁和消息简短。

## 基于 LLM 的工具使用

虽然我们不建议使用任何基于 LLM 的（也常被误导地称为 AI）工具，但我们理解它们将长期存在。

无论你是在报告 bug 还是在贡献代码，你都将对提供的内容承担完全的所有权和责任，你将适用与其他人相同的规则，因此在发送之前请验证 bug 报告或补丁。
